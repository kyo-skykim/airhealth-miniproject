version: 2

sources:
  - name: raw
    description: "Raw zone — views over partitioned parquet (DuckDB) or BigQuery raw dataset."
    schema: raw
    tables:
      - name: air_quality
      - name: weather
      - name: demographics
      - name: health
      - name: county_annual
